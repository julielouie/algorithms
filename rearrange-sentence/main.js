function rearrangeStr(str) {
  const strArr = str.toLowerCase().split(' ');
  console.log(strArr)
}

const sentence = 'The lines are printed in reverse order.';
 // 'In the lines order printed reverse'

console.log(rearrangeStr(sentence));
